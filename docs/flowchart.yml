                    ┌────────────────────────────┐
                    │        🌐 Frontend         │
                    │    (React + Vite App)      │
                    └────────────┬───────────────┘
                                 │
                 ┌───────────────▼──────────────────┐
                 │   Code Input (Monaco Editor)     │
                 │  + Run / Step / Complexity Check │
                 │  + Complexity Visualization Btn  │
                 └──────────────┬───────────────────┘
                                │
                      HTTP Request (JSON)
                    { code, run_code: bool, complexity_check: bool }
                                │
            ┌──────────────────▼──────────────────┐
            │             🔧 Backend              │
            │       (FastAPI or Flask App)        │
            └────────────┬────────────┬───────────┘
                         │            │
     ┌───────────────────▼───┐    ┌───▼────────────────┐
     │ Pre-execution Linter  │    │  🧠 LLM Error       │
     │ - Syntax Validator    │    │  Explanation       │
     │ - Safe Code Checker   │    │  (GPT / CodeT5)     │
     └──────────┬────────────┘    └────────┬───────────┘
                │                          │
     If code is valid                      │
                ▼                          ▼
     ┌────────────────────┐     ┌──────────────────────┐
     │   Execution Tracer │     │    📊 Complexity      │
     │  (sys.settrace or  │     │    Estimator Module   │
     │   AST + custom     │     │  - Uses AST Parser    │
     │   (run_code=true)  │     │  - Static Heuristics  │
     └────────┬───────────┘     │  - LLM Explain (opt)  │
              │                 └──────────┬────────────┘
              │                            │
              └────────────┬───────────────┘
                           │
        ┌──────────────────▼────────────────────────┐
        │   Combine Trace + Complexity + LLM        │
        │   → JSON Output:                           │
        │   { line, vars, stack, timeComplexity,    │
        │     explanation, complexityDetails, ... }│
        └──────────────────┬────────────────────────┘
                           │
          ┌────────────────▼────────────────┐
          │     🎨 Visualization Layer      │
          │   (D3.js / Canvas / Chart.js)   │
          │ - Step-by-step Execution        │
          │ - Variable Tracking & Stack     │
          │ - Complexity Timeline / Labels  │
          │ - Optional: Memory Growth Chart │
          │ - User toggles complexity view │
          └─────────────────────────────────┘
