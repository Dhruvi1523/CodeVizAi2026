                    ┌──────────────────────┐
                    │     🌐 Frontend      │
                    │  (React + Vite App)  │
                    └────────┬─────────────┘
                             │
             ┌───────────────▼────────────────┐
             │     Code Input (Monaco Editor) │
             │  + Run / Step / Check Errors   │
             │  + Complexity Visualization    │
             │    (Button toggle in UI)       │
             └────────┬───────────────────────┘
                      │
              HTTP Request (JSON)
        { code, run_code: bool, visualize_complexity: bool }
                      │
        ┌─────────────▼──────────────┐
        │         🔧 Backend         │
        │   (FastAPI or Flask App)   │
        └───────┬─────────┬──────────┘
                │         │
     ┌──────────▼──┐      │
     │ Pre-execution│     │
     │ LLM Error    │     │
     │ Checker     ─┤     │
     └──────┬──────┘      │
            │             │
  If no errors│           │
            ▼             │
     ┌────────────┐       │
     │  Run Code  │       │
     │ (Tracer.py)│       │
     │ (if run_code=true) │
     └──────┬─────┘       │
            │             │
            ▼             │
     ┌────────────┐       │
     │ Receive    │       │
     │ Trace Data │       │
     └──────┬─────┘       │
            │             │
            │             │
            │      ┌──────▼──────────────┐
            │      │  🤖 LLM Explainer   │
            │      │ (CodeT5, CodeBERT,  │
            │      │  or OpenAI API)     │
            │      └─────┬───────────────┘
            │            │
            ▼            ▼
 ┌─────────────────┐  ┌─────────────────────────────┐
 │ Complexity      │  │ Combine Trace + Explanation  │
 │ Visualization   │  │ into JSON Response           │
 │ Module          │  └──────────────┬──────────────┘
 │ (if visualize_  │                 │
 │ complexity=true)│                 │
 └──────┬──────────┘                 │
        │                            │
        ▼                            ▼
 ┌───────────────┐         ┌─────────────────────────────┐
 │ Complexity    │         │ Send Final Response to      │
 │ Data (AST +   │────────▶│ Frontend (trace, explanation│
 │ rules + opt LLM) │       │ complexity info)            │
 └───────────────┘         └─────────────────────────────┘
